tailwind.config = {
  theme: {
    extend: {
      fontFamily: {
        // 'font-pretendard'라는 클래스명으로 사용할 수 있게 설정합니다.
        // 앞에 배치할수록 우선순위가 높습니다.
        pretendard: ['Pretendard', 'sans-serif'],
      }
    }
  }
}